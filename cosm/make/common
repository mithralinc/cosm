
# Begin common rules

COSM_ROOT = /usr/local
COSM_LIB = $(COSM_ROOT)/lib
COSM_INC = $(COSM_ROOT)/include
COSM_MAN = $(COSM_ROOT)/man

.SUFFIXES:
.SUFFIXES: .c .h .$(OBJ)

# make these spit out errors if undefined
CC_FLAGS = ; echo "ERROR - define CC_FLAGS, follow the templates" ; false
FC_FLAGS = ; echo "ERROR - define FC_FLAGS, follow the templates" ; false
LD_FLAGS = ; echo "ERROR - define LD_FLAGS, follow the templates" ; false
LD_LIBS = ; echo "ERROR - define LD_LIBS, follow the templates" ; false
